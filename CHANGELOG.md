# Changelog - Исправления и Оптимизация

## Дата: 2025-11-18

### 🐛 Исправленные проблемы

#### 1. **Конфликт между несколькими админами** ✅
**Проблема:** Когда два админа одновременно работали с ботом, их действия перезаписывали друг друга из-за общего глобального состояния.

**Решение:**
- Заменили глобальный объект `userState` на `Map<number, UserState>`
- Каждый админ теперь имеет свое изолированное состояние
- Добавлены функции `getUserState()`, `setUserState()`, `clearUserState()` для безопасной работы

**Файлы:** `server/bot/telegram-bot.ts` (строки 157-182)

---

#### 2. **Кеширование APK файлов** ✅
**Проблема:** При обновлении APK файла пользователи скачивали старую закешированную версию.

**Решение:**
- Добавлено поле `lastUpdated` с временной меткой в данные платформы
- Добавлены HTTP заголовки для предотвращения кеширования:
  - `Cache-Control: no-cache, no-store, must-revalidate`
  - `Pragma: no-cache`
  - `Expires: 0`
  - `X-File-Version: <timestamp>`
- Добавлен timestamp в URL скачивания: `/api/download-apk/{platform}?t={timestamp}`

**Файлы:**
- `server/routes/platforms.ts` (строки 73-103)
- `client/pages/Index.tsx` (строки 178-181)
- `server/lib/platform-data.ts` (поле `lastUpdated`)

---

#### 3. **Ссылки не открываются на Android в Telegram** ✅
**Проблема:** При открытии сайта через Telegram на Android, ссылки на букмекерские конторы не открывались.

**Решение:**
- Определяем если сайт открыт в Telegram WebView на Android
- Используем Telegram WebApp API (`window.Telegram.WebApp.openLink()`)
- Добавлен fallback механизм через создание динамической ссылки
- Для обычных браузеров работает стандартный `window.open()`

**Файлы:**
- `client/pages/Index.tsx` (строки 183-212)
- `client/types/telegram-webapp.d.ts` (новый файл с типами)

---

#### 4. **Дублирование кода** ✅
**Проблема:** Функции чтения/записи данных были продублированы в боте и API.

**Решение:**
- Создан общий модуль `server/lib/platform-data.ts`
- Все операции с данными централизованы
- Добавлены новые функции:
  - `readPlatformLinks()` - чтение всех ссылок
  - `writePlatformLinks()` - запись всех ссылок
  - `updatePlatformLink()` - обновление одной ссылки
  - `deletePlatform()` - удаление платформы

**Файлы:**
- `server/lib/platform-data.ts` (новый файл)
- `server/bot/telegram-bot.ts` (использует новый модуль)
- `server/routes/platforms.ts` (использует новый модуль)

---

### 📦 Новые файлы

1. **`server/lib/platform-data.ts`** - Централизованный модуль для работы с данными платформ
2. **`client/types/telegram-webapp.d.ts`** - TypeScript типы для Telegram WebApp API

---

### 🗑️ Удаленные файлы

- `server/bot/telegram-bot-old.ts` - старый backup файл
- `server/bot/telegram-bot.ts.backup` - старый backup файл

---

### ✨ Улучшения

1. **Изолированное состояние для каждого админа** - админы больше не мешают друг другу
2. **Всегда свежие APK файлы** - система кеширования полностью переработана
3. **Работа ссылок на всех платформах** - iOS, Android, Telegram, обычные браузеры
4. **Чистый и поддерживаемый код** - убрано дублирование, добавлена модульность
5. **TypeScript типизация** - добавлены типы для Telegram WebApp

---

### 🧪 Как протестировать

#### Тест 1: Работа нескольких админов одновременно
1. Админ 1 заходит в бот и начинает редактировать платформу "1xbet"
2. Админ 2 заходит в бот и начинает редактировать платформу "melbet"
3. Оба админа должны успешно сохранить свои изменения без конфликтов

#### Тест 2: Обновление APK
1. Админ загружает новый APK файл через бот
2. Пользователь открывает сайт и нажимает кнопку Android
3. Должен скачаться НОВЫЙ APK файл, а не старый из кеша

#### Тест 3: Открытие ссылок на Android
1. Откройте сайт через Telegram на Android
2. Нажмите кнопку "ВОЙТИ" или "iOS"
3. Ссылка должна открыться в браузере, а не зависнуть

#### Тест 4: Открытие ссылок на iOS
1. Откройте сайт через Telegram на iOS
2. Нажмите кнопку "ВОЙТИ" или "iOS"
3. Ссылка должна открыться корректно

---

### 🚀 Деплой на Ubuntu

```bash
# 1. Перейдите в директорию проекта
cd /path/to/project

# 2. Получите обновления из GitHub
git pull origin main

# 3. Установите зависимости (если нужно)
npm install

# 4. Соберите проект
npm run build

# 5. Перезапустите сервер
# Если используете PM2:
pm2 restart all

# Если используете systemd:
sudo systemctl restart your-service-name

# Если используете screen/nohup:
# Сначала остановите старый процесс
kill -9 $(ps aux | grep 'node.*server' | grep -v grep | awk '{print $2}')
# Затем запустите новый
npm start &
```

---

### ⚠️ Важные замечания

1. **Backup данных**: Файл `data/platform-links.json` теперь содержит дополнительное поле `lastUpdated`. Старые данные совместимы.

2. **Переменные окружения**: Убедитесь что в `.env` правильно указаны:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `TELEGRAM_ADMIN_IDS` - ID всех админов через запятую (например: `123456,789012`)

3. **Права доступа**: Убедитесь что папка `data/` доступна для записи

---

### 📝 Техническая документация

#### Структура UserState
```typescript
type UserState = {
  platform?: string;          // Платформа которую редактирует админ
  editType?: 'web' | 'ios' | 'android';  // Тип ссылки
  messageId?: number;         // ID сообщения для редактирования
  chatId?: number;           // ID чата
};
```

#### Структура PlatformData
```typescript
type PlatformData = {
  web: string;               // Ссылка на веб-версию
  ios: string;               // Ссылка на iOS
  android: string;           // Ссылка на Android APK
  androidFileName?: string;  // Имя APK файла
  lastUpdated?: number;      // Timestamp последнего обновления
};
```

---

### 👨‍💻 Контакты для вопросов

Если возникли вопросы или проблемы, проверьте логи:
```bash
# PM2 логи
pm2 logs

# systemd логи
sudo journalctl -u your-service-name -f

# Или просто файл логов если используете nohup
tail -f nohup.out
```
