# 🚀 Развертывание через Git

Git репозиторий создан! Теперь можно легко развернуть проект на хостинге.

## 📦 Что в репозитории

```
✅ Собранное приложение (dist/)
✅ Все логотипы (logos/)
✅ Конфигурация (package.json)
✅ Инструкции по развертыванию
❌ node_modules (будет установлено на сервере)
❌ .env (нужно создать на сервере)
```

---

## Вариант 1: GitHub/GitLab (Рекомендуется)

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com (или зарегистрируйтесь)
2. Нажмите **"New repository"**
3. Введите название: `linkzone`
4. Выберите **Private** (приватный репозиторий)
5. **НЕ создавайте** README, .gitignore (уже есть)
6. Нажмите **"Create repository"**

### Шаг 2: Загрузите код на GitHub

Выполните команды в папке `deploy`:

```bash
# Добавьте ссылку на GitHub (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/linkzone.git

# Загрузите код
git push -u origin master
```

**При запросе логина:**
- Username: ваш GitHub username
- Password: используйте **Personal Access Token** (не пароль!)

**Как получить Token:**
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token → Выберите "repo" права
3. Скопируйте token и используйте вместо пароля

### Шаг 3: Развертывание на сервере из GitHub

**На сервере через SSH:**

```bash
# 1. Клонируйте репозиторий (для приватного нужен token)
git clone https://YOUR_TOKEN@github.com/YOUR_USERNAME/linkzone.git

# 2. Перейдите в папку
cd linkzone

# 3. Установите зависимости
npm install --production

# 4. Создайте .env файл
cp .env.example .env
nano .env
# Настройте токен бота, домен, admin key

# 5. Запустите с PM2
npm install -g pm2
pm2 start dist/server/node-build.mjs --name linkzone
pm2 save
pm2 startup
```

---

## Вариант 2: Прямой Git на хостинге (без GitHub)

Если у вас есть SSH доступ к серверу, можно создать git репозиторий прямо на хостинге.

### На локальном компьютере:

```bash
# В папке deploy
# Добавьте сервер как remote
git remote add production ssh://user@ваш-сервер.uz/home/user/linkzone.git
```

### На сервере:

```bash
# Создайте bare репозиторий
mkdir -p ~/linkzone.git
cd ~/linkzone.git
git init --bare

# Создайте папку для приложения
mkdir -p ~/linkzone-app
```

### Автоматический деплой (post-receive hook):

**На сервере создайте hook:**

```bash
nano ~/linkzone.git/hooks/post-receive
```

**Вставьте:**

```bash
#!/bin/bash
TARGET="/home/user/linkzone-app"
GIT_DIR="/home/user/linkzone.git"

echo "Deploying to $TARGET..."

git --work-tree=$TARGET --git-dir=$GIT_DIR checkout -f

cd $TARGET
npm install --production

echo "Restarting application..."
pm2 restart linkzone || pm2 start dist/server/node-build.mjs --name linkzone

echo "Deployment complete!"
```

**Сделайте исполняемым:**

```bash
chmod +x ~/linkzone.git/hooks/post-receive
```

### Загрузите код:

**На локальном компьютере:**

```bash
git push production master
```

**Автоматически:**
1. Код загрузится на сервер
2. Установятся зависимости
3. Приложение перезапустится

---

## Вариант 3: Загрузка через ZIP (самый простой)

Если нет GitHub аккаунта и сложно с Git:

### На локальном компьютере:

```bash
# В папке deploy создайте ZIP архив
cd "C:\Users\REPOBLIC OF GAMERS\Desktop\deploy"

# Через PowerShell:
Compress-Archive -Path * -DestinationPath ../linkzone-deploy.zip
```

### На сервере:

```bash
# Загрузите linkzone-deploy.zip через FileZilla/WinSCP

# Распакуйте
unzip linkzone-deploy.zip -d linkzone
cd linkzone

# Установите и запустите
npm install --production
cp .env.example .env
nano .env  # Настройте

pm2 start dist/server/node-build.mjs --name linkzone
```

---

## 🔄 Обновление приложения

### Через GitHub:

**На локальном компьютере (после изменений):**

```bash
git add .
git commit -m "Updated platform links"
git push origin master
```

**На сервере:**

```bash
cd linkzone
git pull origin master
npm install --production
pm2 restart linkzone
```

### Через прямой Git:

```bash
# Локально
git push production master

# На сервере всё обновится автоматически (через hook)
```

---

## 🔐 Важно: Безопасность .env

**НИКОГДА не коммитьте .env в git!**

Файл `.env` уже в `.gitignore`, но для безопасности:

### На сервере всегда создавайте .env вручную:

```bash
cp .env.example .env
nano .env

# Вставьте реальные данные:
TELEGRAM_BOT_TOKEN=ваш_настоящий_токен
TELEGRAM_ADMIN_IDS=ваши_id
ADMIN_KEY=секретный_ключ
API_BASE_URL=http://ваш-домен.uz
```

---

## 📋 Чек-лист готовности

### Локально:
- ✅ Git репозиторий создан
- ✅ Файлы закоммичены
- ✅ .env в .gitignore
- ✅ node_modules в .gitignore

### На GitHub/GitLab:
- ⬜ Репозиторий создан (Private!)
- ⬜ Код загружен (git push)

### На сервере:
- ⬜ Код склонирован из git
- ⬜ npm install выполнен
- ⬜ .env создан и настроен
- ⬜ PM2 запущен
- ⬜ Приложение работает

---

## 🎯 Рекомендация

**Лучший вариант для pccloud.uz:**

1. **Загрузите на GitHub** (приватный репозиторий)
2. **На сервере клонируйте** репозиторий
3. **При обновлениях:** git pull на сервере

**Преимущества:**
- ✅ Легко обновлять (git pull)
- ✅ История всех изменений
- ✅ Можно откатить изменения (git checkout)
- ✅ Резервная копия кода на GitHub

---

## 💡 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю
git log --oneline

# Откатить последний коммит (без потери изменений)
git reset --soft HEAD~1

# Откатить изменения в файле
git checkout -- файл

# Создать новую ветку
git checkout -b new-feature

# Переключиться на master
git checkout master

# Объединить ветки
git merge new-feature
```

---

## 📞 Нужна помощь?

Если у вас возникли проблемы:

1. **С GitHub:**
   - Документация: https://docs.github.com/ru
   - Проверьте Personal Access Token

2. **С SSH на сервере:**
   - Свяжитесь с поддержкой pccloud.uz
   - Запросите доступ по SSH

3. **С Git на Windows:**
   - Установите Git: https://git-scm.com/download/win
   - Или используйте GitHub Desktop

---

**Готово! Ваш проект теперь под контролем Git! 🎉**
