# Инструкция по развертыванию на PCCLOUD.UZ

## Подготовка к загрузке

Ваше приложение готово к развертыванию. Размер: **41 MB**

```
deploy/
├── dist/              # Собранное приложение
├── logos/             # Логотипы платформ
├── data/              # Данные бота (будет создана автоматически)
├── node_modules/      # Зависимости (231 пакет, 38 MB)
├── .env               # Конфигурация (ВАЖНО!)
├── package.json       # Конфигурация проекта
├── robots.txt         # SEO файл
└── README.txt         # Общая инструкция
```

---

## Шаг 1: Вход в панель управления PCCLOUD.UZ

1. Откройте https://pccloud.uz/
2. Войдите в личный кабинет
3. Перейдите в раздел управления хостингом

**Важно:** Для Node.js приложений вам нужен один из следующих типов хостинга:
- **VPS хостинг** (рекомендуется) - полный контроль
- **Cloud хостинг** - если поддерживает Node.js
- **Не подходит:** обычный веб-хостинг (только PHP)

---

## Шаг 2: Загрузка файлов

### Вариант A: Через FTP/SFTP (FileZilla, WinSCP)

1. **Получите FTP данные** от pccloud.uz:
   - Хост: `ftp.ваш-домен.uz` или IP адрес
   - Порт: 21 (FTP) или 22 (SFTP)
   - Логин: из панели управления
   - Пароль: из панели управления

2. **Подключитесь через FileZilla:**
   - Скачайте: https://filezilla-project.org/
   - Файл → Менеджер сайтов → Новый сайт
   - Введите данные FTP
   - Протокол: SFTP (безопаснее) или FTP

3. **Загрузите папку deploy:**
   ```
   Локально: C:\Users\REPOBLIC OF GAMERS\Desktop\deploy\
   На сервер: /home/ваш_пользователь/приложение/ или /var/www/
   ```

4. **Важно:** Загрузите ВСЕ файлы, включая скрытые (.env)

### Вариант B: Через SSH (если есть доступ)

```bash
# 1. Подключитесь к серверу
ssh пользователь@ваш-сервер.pccloud.uz

# 2. Создайте директорию
mkdir -p /home/пользователь/linkzone

# 3. На локальном компьютере загрузите файлы
scp -r "C:\Users\REPOBLIC OF GAMERS\Desktop\deploy\*" пользователь@сервер:/home/пользователь/linkzone/
```

---

## Шаг 3: Установка Node.js на сервере

### Проверка Node.js:
```bash
node --version    # Должно быть v18.x или выше
npm --version
```

### Если Node.js не установлен:

**Для Ubuntu/Debian серверов:**
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**Для CentOS/RHEL:**
```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

---

## Шаг 4: Настройка конфигурации

### 1. Редактируйте .env файл:

**Через SSH:**
```bash
cd /home/пользователь/linkzone
nano .env
```

**Или через FTP:** скачайте .env, отредактируйте, загрузите обратно

### 2. Измените настройки:

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=8555264615:AAFHfstEGaW6W_PQBdFN4sbR273qbIYoSxM
TELEGRAM_ADMIN_IDS=5695013277,8095865618

# API Configuration
ADMIN_KEY=ваш-секретный-ключ-смените-его
API_BASE_URL=http://ваш-домен.uz

# Port (если нужен другой)
PORT=8080
```

**ВАЖНО:**
- Смените `ADMIN_KEY` на случайную строку
- Укажите ваш реальный домен в `API_BASE_URL`
- Если хостинг требует конкретный порт, измените `PORT`

---

## Шаг 5: Установка зависимостей (если не загружены)

Если вы НЕ загрузили папку `node_modules`, установите зависимости:

```bash
cd /home/пользователь/linkzone
npm install --production
```

**Это займет 2-3 минуты**

---

## Шаг 6: Запуск приложения

### Тестовый запуск:
```bash
npm start
```

**Вы должны увидеть:**
```
Server running on port 8080
Webhook deleted, starting polling...
Telegram bot started successfully!
```

**Проверьте в браузере:**
```
http://ваш-ip:8080
```

**Если работает** - нажмите Ctrl+C и переходите к следующему шагу.

---

## Шаг 7: Запуск в фоновом режиме

Приложение должно работать постоянно, даже после закрытия SSH.

### Вариант A: PM2 (рекомендуется)

```bash
# 1. Установите PM2
npm install -g pm2

# 2. Запустите приложение
pm2 start dist/server/node-build.mjs --name linkzone

# 3. Автозапуск при перезагрузке сервера
pm2 startup
pm2 save

# 4. Полезные команды
pm2 status          # Статус приложения
pm2 logs linkzone   # Просмотр логов
pm2 restart linkzone # Перезапуск
pm2 stop linkzone   # Остановка
```

### Вариант B: systemd service

Создайте файл сервиса:
```bash
sudo nano /etc/systemd/system/linkzone.service
```

Вставьте:
```ini
[Unit]
Description=LinkZone Application
After=network.target

[Service]
Type=simple
User=ваш-пользователь
WorkingDirectory=/home/пользователь/linkzone
ExecStart=/usr/bin/node dist/server/node-build.mjs
Restart=always
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable linkzone
sudo systemctl start linkzone
sudo systemctl status linkzone
```

### Вариант C: screen (простой способ)

```bash
screen -S linkzone
npm start
# Нажмите Ctrl+A затем D для отсоединения
# screen -r linkzone  # Вернуться к сессии
```

---

## Шаг 8: Настройка Nginx (прокси)

Если у вас есть Nginx, настройте его для работы с Node.js:

```bash
sudo nano /etc/nginx/sites-available/linkzone
```

```nginx
server {
    listen 80;
    server_name ваш-домен.uz www.ваш-домен.uz;

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

Активируйте:
```bash
sudo ln -s /etc/nginx/sites-available/linkzone /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## Шаг 9: Настройка SSL (HTTPS)

### С Certbot (Let's Encrypt - бесплатно):

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d ваш-домен.uz -d www.ваш-домен.uz
```

Сертификат обновится автоматически!

---

## Шаг 10: Настройка домена

В панели управления доменом (или на pccloud.uz):

1. **A-запись:**
   ```
   Тип: A
   Имя: @
   Значение: IP адрес вашего сервера
   TTL: 3600
   ```

2. **WWW-запись:**
   ```
   Тип: CNAME
   Имя: www
   Значение: ваш-домен.uz
   TTL: 3600
   ```

**Распространение DNS занимает 1-24 часа**

---

## Проверка работы

### 1. Проверьте сайт:
```
http://ваш-домен.uz
```

### 2. Проверьте Telegram бота:
- Откройте Telegram
- Найдите вашего бота
- Отправьте `/start`
- Вы должны увидеть меню управления

### 3. Проверьте логи:
```bash
pm2 logs linkzone
# или
sudo journalctl -u linkzone -f
```

---

## Частые проблемы и решения

### ❌ Проблема: "Cannot GET /"

**Решение:**
```bash
# Проверьте, что dist/spa содержит index.html и assets
ls -la dist/spa/
```

### ❌ Проблема: "EADDRINUSE: port already in use"

**Решение:**
```bash
# Найдите процесс на порту 8080
sudo lsof -i :8080
# Убейте процесс
sudo kill -9 PID
```

### ❌ Проблема: Telegram бот не отвечает

**Проверьте:**
1. Токен правильный в .env
2. Бот запущен: `pm2 logs linkzone | grep "Telegram"`
3. Нет других ботов с этим токеном

### ❌ Проблема: "Module not found"

**Решение:**
```bash
cd /home/пользователь/linkzone
rm -rf node_modules
npm install --production
pm2 restart linkzone
```

### ❌ Проблема: 502 Bad Gateway (Nginx)

**Решение:**
```bash
# Проверьте, что приложение работает
pm2 status
# Проверьте порт в nginx конфиге совпадает с PORT в .env
sudo nginx -t
sudo systemctl restart nginx
```

---

## Обновление приложения

Когда нужно обновить код:

```bash
# 1. Остановите приложение
pm2 stop linkzone

# 2. Загрузите новые файлы через FTP
# (замените dist/, client/, server/)

# 3. Пересоберите (если нужно)
npm run build

# 4. Перезапустите
pm2 restart linkzone
```

---

## Резервное копирование

### Важные файлы для бэкапа:

```bash
# 1. Данные бота
data/platform-links.json

# 2. Конфигурация
.env

# 3. Логотипы (если добавили свои)
logos/
```

**Создайте бэкап:**
```bash
tar -czf backup-$(date +%Y%m%d).tar.gz data/ .env logos/
```

---

## Контакты поддержки

**PCCLOUD.UZ:**
- Сайт: https://pccloud.uz
- Email: support@pccloud.uz
- Telegram: @pccloud_support (возможно)

**Если нужна помощь с настройкой:**
1. Проверьте тип хостинга (нужен VPS/Cloud для Node.js)
2. Запросите SSH доступ у поддержки
3. Уточните доступные порты

---

## Итоговая проверка

✅ Node.js установлен (v18+)
✅ Файлы загружены на сервер
✅ .env настроен (токен, домен, порт)
✅ npm install выполнен
✅ Приложение запущено (PM2/systemd)
✅ Nginx настроен (если используется)
✅ Домен указывает на сервер
✅ SSL сертификат установлен
✅ Telegram бот отвечает на /start
✅ Сайт открывается в браузере

**Готово! Ваш LinkZone работает на pccloud.uz!** 🎉
