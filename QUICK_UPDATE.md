# ⚡ Быстрое обновление (5 минут)

## 🎯 Что исправлено
1. Админы не мешают друг другу
2. APK всегда скачивается свежий
3. Ссылки открываются на Android в Telegram
4. Код оптимизирован

---

## 🚀 Команды для обновления

### На вашем компьютере (Windows):

1. **Откройте GitHub Desktop или Git**
2. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Fix: admin conflicts, APK caching, Android links"
   git push origin main
   ```

---

### На сервере Ubuntu:

Скопируйте и вставьте эти команды **одну за одной**:

```bash
# 1. Переход в папку проекта (измените путь если нужно)
cd /var/www/linkzone

# 2. Backup на всякий случай
cp data/platform-links.json data/platform-links.json.backup

# 3. Получить обновления
git pull origin main

# 4. Установить зависимости (если нужно)
npm install

# 5. Собрать проект
npm run build

# 6. Перезапустить сервер (выберите свой вариант)

# Вариант A - PM2 (чаще всего):
pm2 restart all

# Вариант B - systemd:
# sudo systemctl restart your-service-name

# Вариант C - вручную:
# pkill -9 -f "node.*server" && nohup npm start > output.log 2>&1 &

# 7. Проверить что работает
pm2 status
pm2 logs --lines 20
```

---

## ✅ Быстрая проверка

1. **Откройте бота в Telegram** → напишите `/start` → должно показать меню
2. **Откройте сайт** → должен загрузиться
3. **Попробуйте изменить ссылку** через бота → должно сохраниться

---

## ❌ Если что-то не работает

```bash
# Смотрим логи
pm2 logs

# Если там ошибки - пишите мне, покажу как исправить
```

---

## 📝 Важные изменения в коде

### Новые файлы:
- `server/lib/platform-data.ts` - общий модуль для данных
- `client/types/telegram-webapp.d.ts` - типы для Telegram

### Удаленные файлы:
- `server/bot/telegram-bot-old.ts` (backup)
- `server/bot/telegram-bot.ts.backup` (backup)

### Измененные файлы:
- `server/bot/telegram-bot.ts` - исправлен конфликт админов
- `server/routes/platforms.ts` - добавлен cache-busting для APK
- `client/pages/Index.tsx` - исправлено открытие ссылок

---

## 🔥 Одна команда (если используете PM2)

```bash
cd /var/www/linkzone && git pull && npm install && npm run build && pm2 restart all && pm2 logs --lines 20
```

Замените `/var/www/linkzone` на путь к вашему проекту.

---

## 💡 Подсказки

- **Путь к проекту на сервере:** обычно `/var/www/`, `/home/user/`, или `/opt/`
- **Найти путь:** `pwd` (когда находитесь в папке проекта)
- **Проверить PM2:** `pm2 list`
- **Проверить порт:** `netstat -tlnp | grep 8080`

---

## 📞 Если нужна помощь

Скопируйте и пришлите мне вывод этих команд:

```bash
# На сервере:
cd /path/to/project
pwd
ls -la
cat .env | grep -v "TOKEN"
pm2 list
pm2 logs --lines 50
```

Готово! 🎉
