# 🚀 Инструкция по обновлению на Ubuntu сервере

## Что было исправлено

1. ✅ **Конфликт между админами** - теперь несколько админов могут работать одновременно
2. ✅ **Кеширование APK** - пользователи всегда получают последнюю версию APK
3. ✅ **Открытие ссылок на Android** - ссылки теперь открываются корректно в Telegram на Android
4. ✅ **Оптимизация кода** - убрано дублирование, улучшена структура

---

## 📋 Шаг 1: Подключитесь к серверу

```bash
ssh user@your-server-ip
```

---

## 📋 Шаг 2: Перейдите в директорию проекта

```bash
cd /path/to/your/project
# Например: cd /var/www/linkzone
```

---

## 📋 Шаг 3: Создайте backup (на всякий случай)

```bash
# Backup всего проекта
cp -r . ../linkzone-backup-$(date +%Y%m%d)

# Или только backup данных
cp data/platform-links.json data/platform-links.json.backup
```

---

## 📋 Шаг 4: Получите обновления из GitHub

### Вариант A: Если у вас есть GitHub репозиторий

```bash
# Сохраните текущие изменения (если есть)
git stash

# Получите обновления
git pull origin main
# или
git pull origin master

# Примените сохраненные изменения обратно (если нужно)
git stash pop
```

### Вариант B: Если обновляете вручную

Загрузите исправленные файлы на сервер используя `scp` или `ftp`:

```bash
# С вашего локального компьютера:
scp -r server/ user@your-server:/path/to/project/
scp -r client/ user@your-server:/path/to/project/
```

---

## 📋 Шаг 5: Установите зависимости

```bash
# Если package.json изменился
npm install

# Или используйте свой package manager
# pnpm install
# yarn install
```

---

## 📋 Шаг 6: Соберите проект

```bash
npm run build
```

Вы должны увидеть:
```
✓ building client environment for production...
✓ building server environment for production...
✓ built in XXXms
```

---

## 📋 Шаг 7: Перезапустите сервер

### Вариант A: Если используете PM2 (рекомендуется)

```bash
# Просмотр запущенных процессов
pm2 list

# Перезапуск всех процессов
pm2 restart all

# Или перезапуск конкретного приложения
pm2 restart linkzone

# Просмотр логов
pm2 logs
```

### Вариант B: Если используете systemd

```bash
# Перезапуск сервиса
sudo systemctl restart your-service-name

# Проверка статуса
sudo systemctl status your-service-name

# Просмотр логов
sudo journalctl -u your-service-name -f
```

### Вариант C: Если используете screen/tmux/nohup

```bash
# 1. Найдите процесс Node.js
ps aux | grep node

# 2. Убейте старый процесс
kill -9 PROCESS_ID
# Или автоматически:
pkill -9 -f "node.*server"

# 3. Запустите новый процесс
nohup npm start > output.log 2>&1 &

# 4. Проверьте что запустилось
ps aux | grep node
tail -f output.log
```

### Вариант D: Если используете Docker

```bash
# Пересоберите образ
docker-compose build

# Перезапустите контейнеры
docker-compose down
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

---

## 📋 Шаг 8: Проверьте что все работает

### Проверка 1: Сервер запущен

```bash
# Проверьте что процесс работает
ps aux | grep node

# Проверьте что порт открыт (замените 8080 на ваш порт)
netstat -tlnp | grep 8080

# Или используйте curl
curl http://localhost:8080/api/ping
```

Должно вернуть: `{"message":"pong"}`

### Проверка 2: Бот работает

Откройте Telegram и напишите команду `/start` вашему боту.
Должно появиться меню с выбором языка.

### Проверка 3: Сайт работает

Откройте сайт в браузере: `http://your-domain.com`

### Проверка 4: Протестируйте исправления

1. **Тест нескольких админов:**
   - Откройте бота с двух разных аккаунтов админов
   - Попробуйте одновременно редактировать разные платформы
   - Оба должны сохранить изменения успешно

2. **Тест APK кеширования:**
   - Загрузите новый APK через бот
   - Откройте сайт и нажмите кнопку Android
   - Должен скачаться новый файл

3. **Тест ссылок на Android:**
   - Откройте сайт через Telegram на Android
   - Нажмите кнопку "ВОЙТИ" или "iOS"
   - Ссылка должна открыться в браузере

---

## 🔍 Поиск проблем

### Проблема: Сервер не запускается

```bash
# Проверьте логи
pm2 logs
# или
sudo journalctl -u your-service-name -n 50

# Проверьте что порт не занят
sudo lsof -i :8080

# Проверьте права доступа
ls -la data/
# Должна быть папка data/ с правами на запись
```

### Проблема: Бот не отвечает

```bash
# Проверьте токен бота в .env
cat .env | grep TELEGRAM_BOT_TOKEN

# Проверьте ID админов
cat .env | grep TELEGRAM_ADMIN_IDS

# Проверьте логи на ошибки
pm2 logs | grep -i telegram
```

### Проблема: Ссылки не открываются

1. Проверьте что файл `client/types/telegram-webapp.d.ts` существует
2. Проверьте что проект пересобран: `npm run build`
3. Очистите кеш браузера и перезагрузите страницу

---

## 📊 Мониторинг

### PM2 команды для мониторинга

```bash
# Статус всех процессов
pm2 status

# Детальная информация
pm2 show linkzone

# Живые логи
pm2 logs --lines 100

# Мониторинг ресурсов
pm2 monit
```

### Полезные алиасы для .bashrc

Добавьте в `~/.bashrc`:

```bash
alias lz-logs='pm2 logs linkzone'
alias lz-restart='pm2 restart linkzone'
alias lz-status='pm2 status linkzone'
alias lz-update='cd /path/to/project && git pull && npm install && npm run build && pm2 restart all'
```

Затем выполните: `source ~/.bashrc`

---

## 🆘 Откат изменений (если что-то пошло не так)

```bash
# 1. Остановите сервер
pm2 stop all

# 2. Восстановите из backup
rm -rf /path/to/project/*
cp -r ../linkzone-backup-YYYYMMDD/* /path/to/project/

# 3. Запустите старую версию
pm2 restart all

# 4. Сообщите разработчику о проблеме
```

---

## ✅ Чеклист обновления

- [ ] Подключился к серверу
- [ ] Создал backup
- [ ] Получил обновления из GitHub (или загрузил вручную)
- [ ] Запустил `npm install`
- [ ] Запустил `npm run build`
- [ ] Перезапустил сервер
- [ ] Проверил что сервер работает
- [ ] Проверил что бот отвечает
- [ ] Протестировал работу нескольких админов
- [ ] Протестировал загрузку APK
- [ ] Протестировал открытие ссылок на Android

---

## 📞 Контакты

Если возникли проблемы:
1. Проверьте логи сервера
2. Проверьте CHANGELOG.md для деталей изменений
3. Убедитесь что все шаги выполнены правильно

---

## 🎉 Готово!

Ваш сайт и бот обновлены и оптимизированы. Теперь:
- ✅ Несколько админов могут работать одновременно
- ✅ APK всегда загружаются свежие
- ✅ Ссылки открываются на всех платформах
- ✅ Код чище и легче поддерживать

Спасибо за использование LinkZone! 🚀
